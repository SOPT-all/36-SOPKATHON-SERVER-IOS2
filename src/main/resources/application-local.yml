spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/sopkathon" # mysql 직접 접속 후, create database sopkathon으로 db 생성!
    username: "admin"
    password: "0818"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
